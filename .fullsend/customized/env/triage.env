export TRIAGE_SOURCE="${TRIAGE_SOURCE}"
export GITHUB_ISSUE_URL="${GITHUB_ISSUE_URL:-}"
export ISSUE_KEY="${ISSUE_KEY:-}"
export JIRA_HOST="${JIRA_HOST:-}"
export LINKED_GITHUB_REPOS="${LINKED_GITHUB_REPOS:-}"
export GH_TOKEN=${GH_TOKEN}
